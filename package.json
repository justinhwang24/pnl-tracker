{
  "name": "kalshi-pnl-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run build && python3 -m http.server 8000 --bind 127.0.0.1 --directory dist",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "test:browser": "npm run build && playwright test; test_status=$?; env -u SUPABASE_URL -u SUPABASE_PUBLISHABLE_KEY npm run build; exit $test_status"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.28.2"
  },
  "engines": {
    "node": ">=22"
  }
}
